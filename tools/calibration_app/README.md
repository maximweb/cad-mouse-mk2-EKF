# Calibration App (Minimal)

Inteded to record raw sensor data to compare them with a python implementation of
the dipol physics model to check viability.

Might not be working as-is as the serial output hase changed since.